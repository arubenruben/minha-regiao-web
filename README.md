# heldin-internal — Laravel + Inertia.js (React)

Generated by **Laravel Dockerized Installer**.

## Quick start

```bash
# 1. Extract the archive
unzip laravel-heldin-internal-inertia-react-docker.zip
cd heldin-internal

# 2. Build and start the containers
docker compose up --build
```

App: `http://localhost:8080`  
Vite dev server: `http://localhost:5173`

> **This archive is fully ready to use.** The project was created with `laravel new`
> on the server — an `APP_KEY` is already set in `.env` and all Docker files are
> pre-configured.

## What's inside this archive

| File / Folder | Purpose |
|---|---|
| `app/`, `config/`, `routes/`, … | Full Laravel project source |
| `Dockerfile` | PHP 8.4-cli image for the Laravel backend |
| `docker-compose.yml` | App + Vite (Node 20) + postgres + Redis |
| `.env` | Pre-filled environment with a generated `APP_KEY` |
| `.env.docker` | Backup copy of the Docker environment file |

## Project configuration

| Option | Value |
|---|---|
| Starter kit | React |
| Auth provider | Laravel built-in (Breeze) |
| Teams | no |
| Testing | PHPUNIT |
| Database | postgres |
| PHP version | 8.4 |


## Useful commands

```bash
# Stop containers
docker compose down

# Tail app logs
docker compose logs -f app

# Open a shell in the app container
docker compose exec app bash

# Run migrations
docker compose exec app php artisan migrate
```

## Stack

| Service | Image | Host port |
|---|---|---|
| app | php:8.4-cli-alpine | 8080 |
| vite | node:20-alpine | 5173 |
| db | postgres:16-alpine | 5432 |
| redis | redis:7-alpine | 6379 |

